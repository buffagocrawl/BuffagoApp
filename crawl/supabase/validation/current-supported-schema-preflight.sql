-- GENERATED FILE. Source: supabase/contracts/current-supported-schema-v1.json
-- Read-only compatibility preflight. This is not provisioning and not a historical baseline.
begin;
create temp table _buffago_supported_schema_report(check_number integer, schema_name text, object_name text, object_type text, result text, detail text);
insert into _buffago_supported_schema_report values (1, 'pg_catalog', 'pgcrypto', 'extension', case when exists(select 1 from pg_extension where extname='pgcrypto') then 'compatible' else 'missing' end, 'required extension');
insert into _buffago_supported_schema_report values (2, 'public', 'users', 'table', case when to_regclass('public.users') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 2, 'public', 'users.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='users' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 2, 'public', 'users.xp', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'xp' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='users' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 2, 'public', 'users.share_location', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'share_location' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='users' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 2, 'public', 'users.hide_visit_date', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'hide_visit_date' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='users' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 2, 'public', 'users', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = false then 'compatible' else 'incompatible' end, 'RLS required=false' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='users') c;
insert into _buffago_supported_schema_report values (3, 'public', 'crawls', 'table', case when to_regclass('public.crawls') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 3, 'public', 'crawls.crawl_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'crawl_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawls' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 3, 'public', 'crawls.route_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'route_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawls' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 3, 'public', 'crawls.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawls' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 3, 'public', 'crawls.status', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'status' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawls' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 3, 'public', 'crawls', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='crawls') c;
insert into _buffago_supported_schema_report values (4, 'public', 'routes', 'table', case when to_regclass('public.routes') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 4, 'public', 'routes.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='routes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 4, 'public', 'routes.stop1_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'stop1_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='routes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 4, 'public', 'routes.stop2_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'stop2_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='routes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 4, 'public', 'routes.stop3_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'stop3_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='routes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 4, 'public', 'routes.stop4_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'stop4_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='routes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 4, 'public', 'routes.stop5_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'stop5_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='routes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 4, 'public', 'routes', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='routes') c;
insert into _buffago_supported_schema_report values (5, 'public', 'destinations', 'table', case when to_regclass('public.destinations') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 5, 'public', 'destinations.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='destinations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 5, 'public', 'destinations.lat', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'numeric' or c.data_type = 'numeric' then 'compatible' else 'incompatible' end, 'required type numeric' from (select 'lat' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='destinations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 5, 'public', 'destinations.lng', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'numeric' or c.data_type = 'numeric' then 'compatible' else 'incompatible' end, 'required type numeric' from (select 'lng' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='destinations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 5, 'public', 'destinations', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='destinations') c;
insert into _buffago_supported_schema_report values (6, 'public', 'destination_ratings', 'table', case when to_regclass('public.destination_ratings') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 6, 'public', 'destination_ratings.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='destination_ratings' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 6, 'public', 'destination_ratings.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='destination_ratings' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 6, 'public', 'destination_ratings.destination_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'destination_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='destination_ratings' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 6, 'public', 'destination_ratings.crawl_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'crawl_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='destination_ratings' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 6, 'public', 'destination_ratings', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='destination_ratings') c;
insert into _buffago_supported_schema_report select 6, 'public', 'destination_ratings_user_destination_crawl_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'destination_ratings_user_destination_crawl_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='destination_ratings') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report values (7, 'public', 'user_wing_battle_votes', 'table', case when to_regclass('public.user_wing_battle_votes') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 7, 'public', 'user_wing_battle_votes.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_wing_battle_votes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 7, 'public', 'user_wing_battle_votes.battle_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'bigint' or c.data_type = 'bigint' then 'compatible' else 'incompatible' end, 'required type bigint' from (select 'battle_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_wing_battle_votes' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 7, 'public', 'user_wing_battle_votes', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='user_wing_battle_votes') c;
insert into _buffago_supported_schema_report values (8, 'public', 'xp_ledger', 'table', case when to_regclass('public.xp_ledger') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 8, 'public', 'xp_ledger.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='xp_ledger' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 8, 'public', 'xp_ledger.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='xp_ledger' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 8, 'public', 'xp_ledger.amount', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'amount' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='xp_ledger' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 8, 'public', 'xp_ledger.idempotency_key', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'idempotency_key' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='xp_ledger' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 8, 'public', 'xp_ledger', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='xp_ledger') c;
insert into _buffago_supported_schema_report select 8, 'public', 'xp_ledger_idempotency_key_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'xp_ledger_idempotency_key_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='xp_ledger') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report values (9, 'public', 'friendships', 'table', case when to_regclass('public.friendships') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 9, 'public', 'friendships.requester_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'requester_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='friendships' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 9, 'public', 'friendships.addressee_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'addressee_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='friendships' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 9, 'public', 'friendships.status', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'status' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='friendships' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 9, 'public', 'friendships', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='friendships') c;
insert into _buffago_supported_schema_report values (10, 'public', 'mission_assignments', 'table', case when to_regclass('public.mission_assignments') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.mission_key', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'mission_key' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.period_start', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'date' or c.data_type = 'date' then 'compatible' else 'incompatible' end, 'required type date' from (select 'period_start' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.expires_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'expires_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.target', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'target' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.progress', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'progress' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.reward_xp', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'reward_xp' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.period_kind', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'period_kind' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.action_type', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'action_type' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.assignment_timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'assignment_timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.completed_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'completed_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments.claimed_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'claimed_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_assignments' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='mission_assignments') c;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments_user_id_mission_key_period_start_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'mission_assignments_user_id_mission_key_period_start_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='mission_assignments') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 10, 'public', 'mission_assignments_select_own', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'mission_assignments_select_own' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='mission_assignments' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (11, 'public', 'mission_reward_receipts', 'table', case when to_regclass('public.mission_reward_receipts') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 11, 'public', 'mission_reward_receipts.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_reward_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 11, 'public', 'mission_reward_receipts.mission_assignment_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'mission_assignment_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_reward_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 11, 'public', 'mission_reward_receipts.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_reward_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 11, 'public', 'mission_reward_receipts.xp_ledger_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'xp_ledger_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='mission_reward_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 11, 'public', 'mission_reward_receipts', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='mission_reward_receipts') c;
insert into _buffago_supported_schema_report select 11, 'public', 'mission_reward_receipts_mission_assignment_id_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'mission_reward_receipts_mission_assignment_id_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='mission_reward_receipts') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 11, 'public', 'mission_receipts_select_own', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'mission_receipts_select_own' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='mission_reward_receipts' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (12, 'public', 'engagement_mission_definitions', 'table', case when to_regclass('public.engagement_mission_definitions') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.mission_key', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'mission_key' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.period_kind', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'period_kind' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.action_type', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'action_type' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.title', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'title' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.description', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'description' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.target', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'target' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.reward_xp', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'reward_xp' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions.enabled', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'enabled' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_mission_definitions' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_mission_definitions', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='engagement_mission_definitions') c;
insert into _buffago_supported_schema_report select 12, 'public', 'engagement_definitions_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'engagement_definitions_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='engagement_mission_definitions' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (13, 'public', 'engagement_action_receipts', 'table', case when to_regclass('public.engagement_action_receipts') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_action_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_action_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts.action_type', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'action_type' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_action_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts.action_ref', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'action_ref' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_action_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts.occurred_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'occurred_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_action_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts.local_action_date', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'date' or c.data_type = 'date' then 'compatible' else 'incompatible' end, 'required type date' from (select 'local_action_date' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_action_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts.timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_action_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='engagement_action_receipts') c;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_action_receipts_user_id_action_type_action_ref_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'engagement_action_receipts_user_id_action_type_action_ref_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='engagement_action_receipts') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 13, 'public', 'engagement_actions_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'engagement_actions_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='engagement_action_receipts' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (14, 'public', 'user_engagement_streaks', 'table', case when to_regclass('public.user_engagement_streaks') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 14, 'public', 'user_engagement_streaks.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_streaks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 14, 'public', 'user_engagement_streaks.current_streak', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'current_streak' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_streaks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 14, 'public', 'user_engagement_streaks.longest_streak', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'longest_streak' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_streaks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 14, 'public', 'user_engagement_streaks.last_qualified_date', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'date' or c.data_type = 'date' then 'compatible' else 'incompatible' end, 'required type date' from (select 'last_qualified_date' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_streaks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 14, 'public', 'user_engagement_streaks.timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_streaks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 14, 'public', 'user_engagement_streaks', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='user_engagement_streaks') c;
insert into _buffago_supported_schema_report select 14, 'public', 'engagement_streak_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'engagement_streak_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='user_engagement_streaks' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (15, 'public', 'limited_time_events', 'table', case when to_regclass('public.limited_time_events') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.campaign_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'campaign_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.title', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'title' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.description', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'description' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.cta_label', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'cta_label' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.cta_route', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'cta_route' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.starts_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'starts_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.ends_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'ends_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.reward_multiplier', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'numeric' or c.data_type = 'numeric' then 'compatible' else 'incompatible' end, 'required type numeric' from (select 'reward_multiplier' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.eligibility', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'jsonb' or c.data_type = 'jsonb' then 'compatible' else 'incompatible' end, 'required type jsonb' from (select 'eligibility' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.feature_flag', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'feature_flag' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.enabled', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'enabled' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events.environment', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'environment' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='limited_time_events' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='limited_time_events') c;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_time_events_campaign_id_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'limited_time_events_campaign_id_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='limited_time_events') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 15, 'public', 'limited_events_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'limited_events_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='limited_time_events' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (16, 'public', 'user_engagement_preferences', 'table', case when to_regclass('public.user_engagement_preferences') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences.timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences.mission_reminders', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'mission_reminders' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences.streak_reminders', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'streak_reminders' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences.weekly_reminders', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'weekly_reminders' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences.event_alerts', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'event_alerts' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences.push_capable', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'push_capable' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='user_engagement_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 16, 'public', 'user_engagement_preferences', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='user_engagement_preferences') c;
insert into _buffago_supported_schema_report select 16, 'public', 'engagement_preferences_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'engagement_preferences_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='user_engagement_preferences' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (17, 'public', 'in_app_notification_readiness', 'table', case when to_regclass('public.in_app_notification_readiness') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='in_app_notification_readiness' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='in_app_notification_readiness' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness.notification_type', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'notification_type' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='in_app_notification_readiness' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness.source_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'source_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='in_app_notification_readiness' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness.eligible_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'eligible_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='in_app_notification_readiness' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness.expires_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'expires_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='in_app_notification_readiness' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness.read_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'read_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='in_app_notification_readiness' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='in_app_notification_readiness') c;
insert into _buffago_supported_schema_report select 17, 'public', 'in_app_notification_readiness_user_id_notification_type_source_id_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'in_app_notification_readiness_user_id_notification_type_source_id_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='in_app_notification_readiness') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 17, 'public', 'notification_readiness_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'notification_readiness_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='in_app_notification_readiness' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (18, 'public', 'engagement_timezone_state', 'table', case when to_regclass('public.engagement_timezone_state') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 18, 'public', 'engagement_timezone_state.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_timezone_state' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 18, 'public', 'engagement_timezone_state.effective_timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'effective_timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_timezone_state' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 18, 'public', 'engagement_timezone_state.pending_timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'pending_timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_timezone_state' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 18, 'public', 'engagement_timezone_state.pending_since', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'pending_since' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_timezone_state' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 18, 'public', 'engagement_timezone_state.last_changed_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'last_changed_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_timezone_state' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 18, 'public', 'engagement_timezone_state', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='engagement_timezone_state') c;
insert into _buffago_supported_schema_report select 18, 'public', 'engagement_timezone_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'engagement_timezone_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='engagement_timezone_state' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (19, 'public', 'daily_engagement_checks', 'table', case when to_regclass('public.daily_engagement_checks') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='daily_engagement_checks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='daily_engagement_checks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks.local_date', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'date' or c.data_type = 'date' then 'compatible' else 'incompatible' end, 'required type date' from (select 'local_date' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='daily_engagement_checks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks.timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='daily_engagement_checks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks.checked_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'checked_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='daily_engagement_checks' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='daily_engagement_checks') c;
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks_user_id_local_date_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'daily_engagement_checks_user_id_local_date_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='daily_engagement_checks') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 19, 'public', 'daily_engagement_checks_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'daily_engagement_checks_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='daily_engagement_checks' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (20, 'public', 'push_installations', 'table', case when to_regclass('public.push_installations') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.installation_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'installation_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.expo_push_token', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'expo_push_token' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.token_fingerprint', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'token_fingerprint' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.platform', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'platform' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.permission_status', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'permission_status' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.invalidated_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'invalidated_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations.disabled_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'disabled_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='push_installations' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='push_installations') c;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations_user_id_installation_id_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'push_installations_user_id_installation_id_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='push_installations') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 20, 'public', 'push_installations_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'push_installations_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='push_installations' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (21, 'public', 'notification_preferences', 'table', case when to_regclass('public.notification_preferences') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.streak_at_risk', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'streak_at_risk' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.comeback', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'comeback' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.friend_activity', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'friend_activity' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.crawl_proximity', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'crawl_proximity' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.quiet_hours_enabled', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'quiet_hours_enabled' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.quiet_start', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'time without time zone' or c.data_type = 'time without time zone' then 'compatible' else 'incompatible' end, 'required type time without time zone' from (select 'quiet_start' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.quiet_end', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'time without time zone' or c.data_type = 'time without time zone' then 'compatible' else 'incompatible' end, 'required type time without time zone' from (select 'quiet_end' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.reminder_local_time', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'time without time zone' or c.data_type = 'time without time zone' then 'compatible' else 'incompatible' end, 'required type time without time zone' from (select 'reminder_local_time' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences.timezone', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'timezone' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_preferences' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='notification_preferences') c;
insert into _buffago_supported_schema_report select 21, 'public', 'notification_preferences_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'notification_preferences_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='notification_preferences' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (22, 'public', 'notification_outbox', 'table', case when to_regclass('public.notification_outbox') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.event_type', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'event_type' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.source_entity_type', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'source_entity_type' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.source_entity_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'source_entity_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.deduplication_key', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'deduplication_key' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.status', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'status' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.next_attempt_at', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'timestamptz' or c.data_type = 'timestamp with time zone' then 'compatible' else 'incompatible' end, 'required type timestamp with time zone' from (select 'next_attempt_at' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.deep_link', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'deep_link' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox.copy_data', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'jsonb' or c.data_type = 'jsonb' then 'compatible' else 'incompatible' end, 'required type jsonb' from (select 'copy_data' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_outbox' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='notification_outbox') c;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox_user_id_event_type_deduplication_key_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'notification_outbox_user_id_event_type_deduplication_key_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='notification_outbox') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 22, 'public', 'notification_outbox_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'notification_outbox_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='notification_outbox' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (23, 'public', 'notification_delivery_attempts', 'table', case when to_regclass('public.notification_delivery_attempts') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_delivery_attempts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts.outbox_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'outbox_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_delivery_attempts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts.installation_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'installation_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_delivery_attempts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts.attempt_number', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'attempt_number' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_delivery_attempts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts.provider', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'provider' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_delivery_attempts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts.status', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'status' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='notification_delivery_attempts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='notification_delivery_attempts') c;
insert into _buffago_supported_schema_report select 23, 'public', 'notification_delivery_attempts_outbox_id_installation_id_attempt_number_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'notification_delivery_attempts_outbox_id_installation_id_attempt_number_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='notification_delivery_attempts') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report values (24, 'public', 'crawl_proximity_receipts', 'table', case when to_regclass('public.crawl_proximity_receipts') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts.id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawl_proximity_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts.user_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'user_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawl_proximity_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts.crawl_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'crawl_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawl_proximity_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts.destination_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'uuid' or c.data_type = 'uuid' then 'compatible' else 'incompatible' end, 'required type uuid' from (select 'destination_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawl_proximity_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts.installation_id', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'installation_id' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawl_proximity_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts.accuracy_class', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'accuracy_class' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawl_proximity_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts.deduplication_key', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'deduplication_key' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='crawl_proximity_receipts' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='crawl_proximity_receipts') c;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_receipts_deduplication_key_key', 'constraint', case when con.conname is null then 'incompatible' else 'compatible' end, 'required named constraint' from (select 'crawl_proximity_receipts_deduplication_key_key' as required_name) wanted left join (select con.conname from pg_constraint con join pg_class rel on rel.oid=con.conrelid join pg_namespace ns on ns.oid=rel.relnamespace where ns.nspname='public' and rel.relname='crawl_proximity_receipts') con on con.conname=wanted.required_name;
insert into _buffago_supported_schema_report select 24, 'public', 'crawl_proximity_own_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'crawl_proximity_own_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='crawl_proximity_receipts' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (25, 'public', 'engagement_feature_flags', 'table', case when to_regclass('public.engagement_feature_flags') is null then 'missing' else 'present' end, 'required relation');
insert into _buffago_supported_schema_report select 25, 'public', 'engagement_feature_flags.flag_key', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'text' or c.data_type = 'text' then 'compatible' else 'incompatible' end, 'required type text' from (select 'flag_key' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_feature_flags' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 25, 'public', 'engagement_feature_flags.enabled', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'boolean' or c.data_type = 'boolean' then 'compatible' else 'incompatible' end, 'required type boolean' from (select 'enabled' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_feature_flags' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 25, 'public', 'engagement_feature_flags.rollout_percent', 'column', case when c.table_name is null then 'missing' when c.udt_name = 'integer' or c.data_type = 'integer' then 'compatible' else 'incompatible' end, 'required type integer' from (select 'rollout_percent' as required_column) wanted left join information_schema.columns c on c.table_schema='public' and c.table_name='engagement_feature_flags' and c.column_name=wanted.required_column;
insert into _buffago_supported_schema_report select 25, 'public', 'engagement_feature_flags', 'rls', case when c.relname is null then 'missing' when c.relrowsecurity = true then 'compatible' else 'incompatible' end, 'RLS required=true' from (select c.relname, c.relrowsecurity from pg_class c join pg_namespace n on n.oid=c.relnamespace where n.nspname='public' and c.relname='engagement_feature_flags') c;
insert into _buffago_supported_schema_report select 25, 'public', 'engagement_flags_authenticated_read', 'policy', case when p.policyname is null then 'incompatible' else 'compatible' end, 'required policy capability' from (select 'engagement_flags_authenticated_read' as required_name) wanted left join pg_policies p on p.schemaname='public' and p.tablename='engagement_feature_flags' and p.policyname=wanted.required_name;
insert into _buffago_supported_schema_report values (26, 'public', 'award_xp(integer,text,text,uuid,text,uuid,uuid,uuid,bigint,bigint,uuid,uuid,jsonb)', 'function', case when to_regprocedure('public.award_xp(integer,text,text,uuid,text,uuid,uuid,uuid,bigint,bigint,uuid,uuid,jsonb)') is null then 'missing' else 'compatible' end, 'server-authoritative idempotent XP award');
insert into _buffago_supported_schema_report values (27, 'public', 'can_user_appear_socially(uuid)', 'function', case when to_regprocedure('public.can_user_appear_socially(uuid)') is null then 'missing' else 'compatible' end, 'social privacy eligibility');
insert into _buffago_supported_schema_report values (28, 'public', 'friend_pair_is_blocked(uuid,uuid)', 'function', case when to_regprocedure('public.friend_pair_is_blocked(uuid,uuid)') is null then 'missing' else 'compatible' end, 'block relationship eligibility');
insert into _buffago_supported_schema_report values (29, 'public', 'destination_rating_friend_notification', 'trigger', case when exists(select 1 from pg_trigger t join pg_class r on r.oid=t.tgrelid join pg_namespace ns on ns.oid=r.relnamespace where ns.nspname='public' and r.relname='destination_ratings' and t.tgname='destination_rating_friend_notification' and not t.tgisinternal) then 'compatible' else 'missing' end, 'friend rating enqueue trigger');
do $$ declare missing_count integer; incompatible_count integer; begin select count(*) filter(where result='missing'), count(*) filter(where result='incompatible') into missing_count,incompatible_count from _buffago_supported_schema_report; raise notice 'current_supported_schema_contract_version=1.0.0 generated_check_count=29 missing=% incompatible=%', missing_count,incompatible_count; if missing_count > 0 or incompatible_count > 0 then raise exception 'current_supported_schema_preflight_failed missing=% incompatible=% generated_check_count=29', missing_count,incompatible_count; end if; end $$;
select * from _buffago_supported_schema_report order by check_number, object_type, object_name;
rollback;
