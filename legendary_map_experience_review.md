# Legendary map experience review

The showcase marker uses a star symbol, the word `LEGENDARY`, a distinct shape, selection scale change, and a selected callout with restaurant, time, reason, and one action. Cluster state uses `✦3` plus `LEGENDARY STOPS`; ordinary marker color is not required to interpret Legendary status. The production adapter remains a release integration task unless a live event feed is enabled.

