# Phase 2 runtime evidence chain

Migration integrity, exact Phase 1/2 bytes, remote ledger, live schema, zero production rows, disabled flags, typecheck, relevant tests, Expo Doctor 18/18, and web export are accepted factual prerequisites. Repository integrity is not an open gate unless new drift appears.

The notification boundary is now complete and deployed disabled through
corrective migration `20260724140000`. Exact live counts remain zero for event
instances, all Legendary tables, and the notification outbox. The remote
ledger records Phase 1, Phase 2, and the corrective migration exactly once.
All 14 Buffaverse flags remain disabled.

Repository-controlled runtime evidence now covers exact migration hashes,
unique constraints, advisory locks in the immutable Phase 2 migration,
notification preference/cap/dedupe/expiry/cancellation/eligibility behavior,
deterministic performance, real Home/Wingdex/detail integration, and
disabled-by-default client gates.

Remaining approval gates are fresh screenshot/browser interaction evidence,
semantic browser accessibility output, and a fresh blind product panel. No
production inserts, flag enablement, or Phase 3 work is permitted before
Phase 2 approval.
