# Repository Evidence

This folder contains historical review packets, rendered UX evidence, scorecards, and approval notes that are useful context but are not primary application documentation.

New Serrano runs write their generated evidence to `Agents/Serrano/runs/<run-id>/evidence/`. Keep repository-level evidence here when it needs to remain available across runs; do not add evidence Markdown files to the repository root.
