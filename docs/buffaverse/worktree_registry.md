# Worktree registry

Continuation worktree: `C:\Users\Brand\repo\BuffagoApp-phase2-approval` on `workstream/buffaverse-phase2-approval-reconciliation`.

The checked-out release candidate is intentionally not the Buffaverse continuation worktree.
