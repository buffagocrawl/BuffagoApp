# Shared evidence harness

Deterministic fixtures cover critical state models and static accessibility semantics. SQL contract tests inspect migration provenance, flags, grants, RLS, reward boundaries, and bounded notification behavior. Rendered screenshots, physical-device checks, and true screen-reader evidence are pending manual user validation.
