# Mobile onboarding results

ONBOARD-001: **BLOCKED**. The clean first-time journey was not performed by the owner on a physical release device. Existing local/runtime evidence is not sufficient to score taps, completion time, confusion, permissions, back behavior, or persisted XP/streak/reward state.

Required owner evidence covers launch → auth → proposition → state → restaurant → valid rating → reward → home → crawl preview → streak → Buffaverse.
