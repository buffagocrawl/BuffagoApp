# Release gates

Serrano approves only grounded runtime evidence. `REJECT` means a confirmed product defect. `BLOCKED` means an external/device/fixture dependency prevented execution. `INSUFFICIENT_EVIDENCE` means the run was incomplete or its evidence contract was not sufficient. Screenshot baselines are advisory until an explicitly release-gating checkpoint has a matching device profile and an intentionally approved baseline.
