# Fixture summary

The allowlist contains the requested user-state fixtures. The guard requires a non-production environment and a
`cayenne-*@qa.buffago.test` identity. No arbitrary SQL interface is exposed.

