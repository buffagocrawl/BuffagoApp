# File change inventory

Added:

- `Agents/Cayenne/`: Python contracts/runtime/fixture guard/Serrano adapter, JSON schemas, Maestro contracts, tests, and README.
- `Agents/Serrano/serrano/cayenne_integration.py`: Serrano invocation and result ingestion boundary.
- `scripts/cayenne/`: Windows prerequisite, run, cleanup, and report commands.
- `crawl/lib/cayenneTestMode.ts`, `crawl/lib/cayenneSelectors.ts`: guarded runtime metadata and stable selector registry.
- `artifacts/cayenne-build/`: requested architecture, contract, security, validation, limitation, and operator documentation.

Modified:

- `Agents/Serrano/serrano/cli.py`: runtime-validation and runtime-result commands, BOM-safe input, and structured non-discovery output handling.

Preserved pre-existing user changes:

- Referral route, migration reconciliation, deployment migration docs, referral route tests, and `docs/reviews/`.

